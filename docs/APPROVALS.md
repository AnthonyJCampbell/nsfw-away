# Manual Approvals Required

No global or system-level commands are required for this project. The extension is pure vanilla JS/HTML/CSS with zero build step and zero dependencies.

## Icon Generation (Optional)
- Command: `node scripts/generate-icons.js`
- Scope: Local only, generates PNG files in icons/ directory
- Risk: None — only writes to project directory
- Status: No approval needed (local operation)
